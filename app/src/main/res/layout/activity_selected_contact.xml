<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_selected_contact"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.whf.messagerelayer.activity.SelectedContactActivity">
    <android.support.v7.widget.RecyclerView
        android:id="@+id/list_selected_contact"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/button_add_one">
    </android.support.v7.widget.RecyclerView>
    <Button
        android:id="@+id/button_add_one"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="0dp"
        android:layout_marginRight="15dp"
        android:layout_marginLeft="15dp"
        android:layout_marginBottom="10dp"
        android:layout_above="@+id/button_add_contact"
        android:background="@drawable/bg_add_contact"
        android:text="自行输入添加"
        android:textSize="16sp"
        android:textColor="@android:color/white"/>
    <Button
        android:id="@+id/button_add_contact"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="0dp"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:layout_marginBottom="15dp"
        android:layout_alignParentBottom="true"
        android:background="@drawable/bg_add_one"
        android:text="从通讯录添加"
        android:textSize="16sp"
        android:textColor="@color/colorLightText"/>
</RelativeLayout>
